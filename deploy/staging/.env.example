# Staging 环境配置
# 复制此文件为 .env 并填入实际配置
# 文件权限应设置为 600: chmod 600 .env

# ==================== Docker Registry ====================
DOCKER_REGISTRY=registry.cn-hangzhou.aliyuncs.com
DOCKER_NAMESPACE={{NAME}}
IMAGE_TAG=latest

# ==================== PostgreSQL ====================
POSTGRES_DB={{NAME}}_staging
POSTGRES_USER={{NAME}}
# 使用强密码，至少 16 字符
# 生成命令: openssl rand -base64 32 | tr -dc 'a-zA-Z0-9' | head -c 32
POSTGRES_PASSWORD=

# ==================== Redis ====================
# 使用强密码，至少 16 字符
REDIS_PASSWORD=

# ==================== JWT ====================
# 至少 32 字符的随机字符串
# 生成命令: openssl rand -base64 64 | tr -d '\n'
JWT_ACCESS_SECRET=
JWT_REFRESH_SECRET=

# ==================== CORS ====================
# 逗号分隔的允许域名
CORS_ORIGINS=https://admin-staging.{{DOMAIN}},https://www-staging.{{DOMAIN}}

# 服务端环境配置
# 复制此文件为 .env 并填入实际配置

# ==================== 服务配置 ====================
PORT={{PORT_SERVER_DEV}}
NODE_ENV=development

# ==================== 数据库 ====================
# 开发环境默认配置，生产环境请使用强密码
DATABASE_URL=postgresql://{{NAME}}:{{NAME}}Dev_ChangeThisPassword@localhost:{{PORT_POSTGRES_DEV}}/{{NAME}}_dev

# ==================== Redis ====================
REDIS_URL=redis://:{{NAME}}RedisDev_ChangeThisPassword@localhost:{{PORT_REDIS_DEV}}/0

# ==================== JWT ====================
# 开发环境使用固定密钥，生产环境请使用 openssl rand -base64 64 生成
JWT_ACCESS_SECRET=dev-access-secret-change-in-production
JWT_REFRESH_SECRET=dev-refresh-secret-change-in-production

# ==================== CORS ====================
# 开发环境允许的前端地址
CORS_ORIGINS=http://localhost:{{PORT_ADMIN_DEV}},http://localhost:{{PORT_WWW_DEV}}

# 微信小程序（Taro）环境配置（构建期）
# 复制此文件为 .env 并填入实际配置（如需）

# 构建脚本环境（由 package.json scripts 使用）
# - development -> APP_ENV=dev
# - staging     -> APP_ENV=staging
# - production  -> APP_ENV=prod
TARO_APP_ENV=development

